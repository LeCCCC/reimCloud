<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.reimcloud.mapper.ReimSubsidyMapper">

    <resultMap id="ReimSubsidyMap" type="org.example.reimcloud.entity.ReimSubsidy">
        <id column="id" property="id"/>
        <result column="main_id" property="mainId"/>
        <result column="traveler_id" property="travelerId"/>
        <result column="traveler_no" property="travelerNo"/>
        <result column="traveler_name" property="travelerName"/>
        <result column="departure_date" property="departureDate"/>
        <result column="arrival_date" property="arrivalDate"/>
        <result column="subsidy_days" property="subsidyDays"/>
        <result column="departure_city" property="departureCity"/>
        <result column="departure_city_no" property="departureCityNo"/>
        <result column="arriving_city" property="arrivingCity"/>
        <result column="arriving_city_no" property="arrivingCityNo"/>
        <result column="application_amount" property="applicationAmount"/>
        <result column="subsidy_amount" property="subsidyAmount"/>
        <result column="meal_allowance" property="mealAllowance"/>
        <result column="transportation_allowance" property="transportationAllowance"/>
        <result column="phone_allowance" property="phoneAllowance"/>
        <result column="business_type_id" property="businessTypeId"/>
        <result column="business_type_no" property="businessTypeNo"/>
        <result column="business_type_name" property="businessTypeName"/>
    </resultMap>

    <insert id="insert" parameterType="org.example.reimcloud.entity.ReimSubsidy">
        insert into fk_reim_subsidy (
            id, main_id, traveler_id, traveler_no, traveler_name,
            departure_date, arrival_date, subsidy_days,
            departure_city, departure_city_no, arriving_city, arriving_city_no,
            application_amount, subsidy_amount, meal_allowance,
            transportation_allowance, phone_allowance,
            business_type_id, business_type_no, business_type_name
        ) values (
            #{id}, #{mainId}, #{travelerId}, #{travelerNo}, #{travelerName},
            #{departureDate}, #{arrivalDate}, #{subsidyDays},
            #{departureCity}, #{departureCityNo}, #{arrivingCity}, #{arrivingCityNo},
            #{applicationAmount}, #{subsidyAmount}, #{mealAllowance},
            #{transportationAllowance}, #{phoneAllowance},
            #{businessTypeId}, #{businessTypeNo}, #{businessTypeName}
        )
    </insert>

    <delete id="deleteByMainId">
        delete from fk_reim_subsidy
        where main_id = #{mainId}
    </delete>

    <select id="selectByMainId" resultMap="ReimSubsidyMap">
        select id, main_id, traveler_id, traveler_no, traveler_name,
               departure_date, arrival_date, subsidy_days,
               departure_city, departure_city_no, arriving_city, arriving_city_no,
               application_amount, subsidy_amount, meal_allowance,
               transportation_allowance, phone_allowance,
               business_type_id, business_type_no, business_type_name
        from fk_reim_subsidy
        where main_id = #{mainId}
        order by traveler_name, departure_date, arrival_date
    </select>
</mapper>
